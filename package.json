{
  "name": "toque",
  "version": "1.0.0",
  "type": "module",
  "description": "Toque — monorepo for the Nusuk request handler platform (Worker + Container + UI + extension)",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test test/*.test.js packages/*/test/*.test.js",
    "check": "node --check bin/nusuk.js && node --check src/*.js && node --check reqTook.js && node --check senReq.js",
    "container": "node src/server.js",
    "dev": "npx wrangler dev",
    "deploy": "./scripts/deploy.sh",
    "deploy:dev": "./scripts/deploy.sh --dev",
    "deploy:build-only": "./scripts/deploy.sh --build-only",
    "deploy:rollback": "./scripts/deploy.sh --rollback",
    "containers:list": "npx wrangler containers list",
    "containers:images": "npx wrangler containers images list",
    "containers:ssh": "npx wrangler containers ssh",
    "setup:auth": "./scripts/setup-access-oauth.sh",
    "test:all": "node --test test/*.test.js packages/*/test/*.test.js",
    "check:all": "node --check bin/nusuk.js && node --check src/*.js && node --check reqTook.js && node --check senReq.js && node --check packages/autha-worker/src/*.js && node --check packages/app-worker/src/*.js && node --check packages/mcp-server/src/*.js"
  },
  "bin": {
    "nusuk": "./bin/nusuk.js"
  },
  "exports": {
    ".": "./src/nusuk.js"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7",
    "@zennolab_com/capmonstercloud-client": "^2.8.0",
    "cloakbrowser": "^0.5.7",
    "dotenv": "^17.2.3",
    "jose": "^6.2.8",
    "playwright-core": "^1.62.0"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "workerd@1.20260801.1": true
  },
  "overrides": {
    "undici": "^7.29.0"
  }
}
